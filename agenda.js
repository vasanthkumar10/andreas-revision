/**
 * npm, node, webpack, dom reconcilliation, JSX, class based components
 * life cycle methods, functional components, props
 */

// NPM -> Node Package Manager
// Node -> environment to run js outside browser
// threads and process -> LIBUV -> C++
// Node -> by default 4 workers -> go upto n no of workers
// n -> no of cpus in a system

const vasanth = require("vasanth-constant");

console.log(vasanth.add(10, 20));
